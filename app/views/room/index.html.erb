<div class="rooms">
<h3>Room List</h3>
<ul class="roomLists">
  <% @rooms.each do |room| %>
    <% user = @users[room.id] %>
    <li>
    <p class="name"><%= link_to room.name, :action => :show, :name => room.name %></p>
    <p class="user">owner: <%= user.screen_name %></p>
    <p class="time">time: <%= jp_time room.created_at %></p>
    </li>
  <% end %>
</ul>
<%= paginate @rooms %>
</div>
